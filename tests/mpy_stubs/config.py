# Stub config for off-device tests. Listed before the repo root on
# MICROPYPATH so tests never read the real (gitignored) config.py.
WIFI_SSID = "test-ssid"
WIFI_PASSWORD = "test-pass"
HA_URL = "http://192.0.2.1:8123"
HA_TOKEN = "test-token"
DEBUG = False
